                    first = -1;
            if (i == size) {
                if (first == size) {
                break;
            }
            if (found) {
            cout << i << " " << first << " " << last << endl;
        for (int i = 1; i < (size + 1); i++) {
        int size = nums.size();
        int failed = 0;
        int last = 0;
        int first = 0;
        int found = 0;
public:
    vector<int> twoSum(vector<int>& nums, int target) {
class Solution {
